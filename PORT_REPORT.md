# Port report — `flate2-kotlin`

> Generated by `scaffold/generate_repo_reports.py` from `workspace_dep_graph.json` ⨯ `port_priority.json`. Do not hand-edit. Re-run after the workspace dep graph is rebuilt.

## Overview

- **1** member crate(s): `flate2`
- **Codex-kotlin distance**: **hop 1** from codex-kotlin via cross-repo Rust deps
- **Inbound**: 36 `use` sites from 8 other repos
- **Outbound**: 7 `use` sites to 1 other repos
- **Unresolved external**: 6 published Cargo crates with no `*-kotlin/` port

## Member crates

| package | module | inbound `use` sites | outbound external repos | Jira | rank | status |
|---|---|---:|---:|---|---:|---|
| `flate2` | `flate2` | 36 | 1 | COD-53 | 36 | To Do |

## Inbound — what to port first

Every symbol this repo's crates need to expose, ranked by total workspace demand. **Codex-kotlin sites are listed first per symbol** because codex is the priority consumer driving the port queue. Anything not listed here is not currently `use`'d by any kotlinmania repo and can be deferred.

### Inbound caller summary (36 edges, 9 caller crates)

| caller repo | caller crate | use sites | codex consumer? |
|---|---|---:|---|
| `rama-http-kotlin` | `rama-http` | 8 |  |
| `tungstenite-kotlin` | `tungstenite` | 6 |  |
| `codex-kotlin` | `codex-core-plugins` | 5 | ✅ |
| `codex-kotlin` | `codex-app-server` | 4 | ✅ |
| `tonic-kotlin` | `tonic` | 4 |  |
| `opentelemetry-otlp-kotlin` | `opentelemetry-otlp` | 3 |  |
| `syntect-kotlin` | `syntect` | 3 |  |
| `zip-kotlin` | `zip` | 2 |  |
| `syn-kotlin` | `syn` | 1 |  |

### Symbol-level demand (16 distinct symbols)

Sorted by codex demand, then total demand. Symbols with codex-kotlin sites are the priority queue. Each symbol entry below shows up to 12 call sites (codex first).

| symbol | total | codex | other | distinct callers |
|---|---:|---:|---:|---:|
| `flate2::Compression` | 9 | 4 | 5 | 7 |
| `flate2::write::GzEncoder` | 6 | 4 | 2 | 4 |
| `flate2::read::GzDecoder` | 5 | 1 | 4 | 5 |
| `flate2::bufread::DeflateDecoder` | 2 | 0 | 2 | 1 |
| `flate2::bufread::GzDecoder` | 2 | 0 | 2 | 1 |
| `flate2::write::DeflateEncoder` | 2 | 0 | 2 | 2 |
| `flate2::Compress` | 1 | 0 | 1 | 1 |
| `flate2::Decompress` | 1 | 0 | 1 | 1 |
| `flate2::FlushCompress` | 1 | 0 | 1 | 1 |
| `flate2::FlushDecompress` | 1 | 0 | 1 | 1 |
| `flate2::Status` | 1 | 0 | 1 | 1 |
| `flate2::bufread::ZlibDecoder` | 1 | 0 | 1 | 1 |
| `flate2::read::GzEncoder` | 1 | 0 | 1 | 1 |
| `flate2::read::ZlibDecoder` | 1 | 0 | 1 | 1 |
| `flate2::read::ZlibEncoder` | 1 | 0 | 1 | 1 |
| `flate2::write::ZlibEncoder` | 1 | 0 | 1 | 1 |

#### Per-symbol detail (top 16 symbols)

##### `flate2::Compression` (9 sites, 4 codex, 5 other, 7 caller crates)

Codex call sites:

- `codex-kotlin/codex-app-server` `tests/suite/v2/plugin_install.rs:32` — `use flate2::Compression`
- `codex-kotlin/codex-app-server` `tests/suite/v2/plugin_list.rs:22` — `use flate2::Compression`
- `codex-kotlin/codex-core-plugins` `src/remote/share.rs:5` — `use flate2::Compression`
- `codex-kotlin/codex-core-plugins` `src/remote_bundle.rs:539` — `use flate2::Compression`

Other workspace call sites:

- `opentelemetry-otlp-kotlin/opentelemetry-otlp` `src/exporter/http/mod.rs:318` — `use flate2::Compression`
- `rama-http-kotlin/rama-http` `src/layer/decompression/request/mod.rs:12` — `use flate2::Compression`
- `syntect-kotlin/syntect` `src/dumps.rs:29` — `use flate2::Compression`
- `tungstenite-kotlin/tungstenite` `src/extensions/compression/deflate/config.rs:2` — `use flate2::Compression`
- `zip-kotlin/zip` `src/write.rs:33` — `use flate2::Compression`

##### `flate2::write::GzEncoder` (6 sites, 4 codex, 2 other, 4 caller crates)

Codex call sites:

- `codex-kotlin/codex-app-server` `tests/suite/v2/plugin_install.rs:33` — `use flate2::write::GzEncoder`
- `codex-kotlin/codex-app-server` `tests/suite/v2/plugin_list.rs:23` — `use flate2::write::GzEncoder`
- `codex-kotlin/codex-core-plugins` `src/remote/share.rs:6` — `use flate2::write::GzEncoder`
- `codex-kotlin/codex-core-plugins` `src/remote_bundle.rs:540` — `use flate2::write::GzEncoder`

Other workspace call sites:

- `opentelemetry-otlp-kotlin/opentelemetry-otlp` `src/exporter/http/mod.rs:318` — `use flate2::write::GzEncoder`
- `rama-http-kotlin/rama-http` `src/layer/decompression/request/mod.rs:12` — `use flate2::write::GzEncoder`

##### `flate2::read::GzDecoder` (5 sites, 1 codex, 4 other, 5 caller crates)

Codex call sites:

- `codex-kotlin/codex-core-plugins` `src/remote_bundle.rs:10` — `use flate2::read::GzDecoder`

Other workspace call sites:

- `opentelemetry-otlp-kotlin/opentelemetry-otlp` `src/exporter/http/mod.rs:829` — `use flate2::read::GzDecoder`
- `rama-http-kotlin/rama-http` `src/layer/compression/mod.rs:108` — `use flate2::read::GzDecoder`
- `syn-kotlin/syn` `tests/repo/mod.rs:7` — `use flate2::read::GzDecoder`
- `tonic-kotlin/tonic` `src/codec/compression.rs:4` — `use flate2::read::GzDecoder`

##### `flate2::bufread::DeflateDecoder` (2 sites, 0 codex, 2 other, 1 caller crates)

Other workspace call sites:

- `rama-http-kotlin/rama-http` `src/service/fs/serve_dir/tests.rs:7` — `use flate2::bufread::DeflateDecoder`
- `rama-http-kotlin/rama-http` `src/service/fs/serve_file.rs:180` — `use flate2::bufread::DeflateDecoder`

##### `flate2::bufread::GzDecoder` (2 sites, 0 codex, 2 other, 1 caller crates)

Other workspace call sites:

- `rama-http-kotlin/rama-http` `src/service/fs/serve_dir/tests.rs:7` — `use flate2::bufread::GzDecoder`
- `rama-http-kotlin/rama-http` `src/service/fs/serve_file.rs:181` — `use flate2::bufread::GzDecoder`

##### `flate2::write::DeflateEncoder` (2 sites, 0 codex, 2 other, 2 caller crates)

Other workspace call sites:

- `rama-http-kotlin/rama-http` `src/body/zip_bomb.rs:2` — `use flate2::write::DeflateEncoder`
- `zip-kotlin/zip` `src/write.rs:33` — `use flate2::write::DeflateEncoder`

##### `flate2::Compress` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `tungstenite-kotlin/tungstenite` `src/extensions/compression/deflate/mod.rs:5` — `use flate2::Compress`

##### `flate2::Decompress` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `tungstenite-kotlin/tungstenite` `src/extensions/compression/deflate/mod.rs:5` — `use flate2::Decompress`

##### `flate2::FlushCompress` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `tungstenite-kotlin/tungstenite` `src/extensions/compression/deflate/mod.rs:5` — `use flate2::FlushCompress`

##### `flate2::FlushDecompress` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `tungstenite-kotlin/tungstenite` `src/extensions/compression/deflate/mod.rs:5` — `use flate2::FlushDecompress`

##### `flate2::Status` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `tungstenite-kotlin/tungstenite` `src/extensions/compression/deflate/mod.rs:5` — `use flate2::Status`

##### `flate2::bufread::ZlibDecoder` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `syntect-kotlin/syntect` `src/dumps.rs:25` — `use flate2::bufread::ZlibDecoder`

##### `flate2::read::GzEncoder` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `tonic-kotlin/tonic` `src/codec/compression.rs:4` — `use flate2::read::GzEncoder`

##### `flate2::read::ZlibDecoder` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `tonic-kotlin/tonic` `src/codec/compression.rs:6` — `use flate2::read::ZlibDecoder`

##### `flate2::read::ZlibEncoder` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `tonic-kotlin/tonic` `src/codec/compression.rs:6` — `use flate2::read::ZlibEncoder`

##### `flate2::write::ZlibEncoder` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `syntect-kotlin/syntect` `src/dumps.rs:27` — `use flate2::write::ZlibEncoder`

## Outbound — what this repo depends on

Other kotlinmania repos this repo's Rust source `use`s. Symbol-level detail per target lives in **that target repo's PORT_REPORT.md inbound section** (no duplication). Sorted by Jira rank, then by `use`-site count.

| target repo | use sites | distinct symbols | target packages | Jira | rank | status |
|---|---:|---:|---|---|---:|---|
| `rand-kotlin` | 7 | 2 | `rand` | COD-114 | 14 | To Do |

## Unresolved external dependencies

Published Cargo crates this repo's Rust source `use`s but for which no `<crate>-kotlin/` repo exists. Each is either a missing port to file in Jira, or an intentional external dep (system library / FFI / build-only). If a port lives at a non-conventional repo name, add it to `PACKAGE_REPO_OVERRIDES` in `scaffold/build_workspace_dep_graph.py`.

| package | edges | distinct symbols | importing members in this repo |
|---|---:|---:|---|
| `zlib-rs` | 8 | 8 | `flate2` |
| `miniz_oxide` | 8 | 8 | `flate2` |
| `crc32fast` | 3 | 1 | `flate2` |
| `libz-ng-sys` | 1 | 1 | `flate2` |
| `cloudflare-zlib-sys` | 1 | 1 | `flate2` |
| `libz-sys` | 1 | 1 | `flate2` |
