# Port targets — `flate2-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `flate2` that codex-rs uses, and therefore the minimum subset that `flate2-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-53] (rank `36`, tier `direct`, status `To Do`)

**Cargo package name**: `flate2`
**Rust module name (in `use` statements)**: `flate2`

## Summary

- **3** distinct symbols imported by codex-rs
- **9** total `use` statements
- **4** codex source files affected
- **2** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `flate2::Compression`

- **4 `use` site(s)** across **2 codex member crate(s)**: `codex-app-server`, `codex-core-plugins`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `app-server/tests/suite/v2/plugin_install.rs:32` (`codex-app-server`) — `use flate2::Compression`
- `app-server/tests/suite/v2/plugin_list.rs:22` (`codex-app-server`) — `use flate2::Compression`
- `core-plugins/src/remote/share.rs:5` (`codex-core-plugins`) — `use flate2::Compression`
- `core-plugins/src/remote_bundle.rs:539` (`codex-core-plugins`) — `use flate2::Compression`

### `flate2::write::GzEncoder`

- **4 `use` site(s)** across **2 codex member crate(s)**: `codex-app-server`, `codex-core-plugins`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `app-server/tests/suite/v2/plugin_install.rs:33` (`codex-app-server`) — `use flate2::write::GzEncoder`
- `app-server/tests/suite/v2/plugin_list.rs:23` (`codex-app-server`) — `use flate2::write::GzEncoder`
- `core-plugins/src/remote/share.rs:6` (`codex-core-plugins`) — `use flate2::write::GzEncoder`
- `core-plugins/src/remote_bundle.rs:540` (`codex-core-plugins`) — `use flate2::write::GzEncoder`

### `flate2::read::GzDecoder`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-core-plugins`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core-plugins/src/remote_bundle.rs:10` (`codex-core-plugins`) — `use flate2::read::GzDecoder`
